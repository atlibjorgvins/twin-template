<script lang="ts">
  // The load() in +page.ts redirects to /calendar/grid before this
  // renders — kept only so the route resolves. The unified grid (with
  // per-type filter chips) replaced the old landing tiles.
</script>

<svelte:head><title>Calendar · Hub</title></svelte:head>

<p class="p-4 text-sm text-ink-500">Opening the calendar…</p>
