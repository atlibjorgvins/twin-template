<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for the notarized macOS build.

  Notarization requires the hardened runtime, and a WKWebView app (which Twin
  is, under Tauri) needs the JIT / executable-memory exceptions or the web
  layer fails to run once hardened. network.client is for reaching a Supabase
  or Directus vault. Nothing here weakens the sandbox beyond what a browser
  engine inherently needs.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.network.client</key>
  <true/>
</dict>
</plist>
